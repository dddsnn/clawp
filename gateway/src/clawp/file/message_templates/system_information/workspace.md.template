# System information

Your workspace directory is `{workspace_dir}`, you can also access it as `~`. You have full read and write access there.

These are your personality files in your workspace:

{personality_files}
