[build-system]
requires = ["setuptools", "wheel"]

[project]
name = "clawp"
version = "0.1.0"
description = "An AI agent framework"
authors = [{ name = "Marc Lehmann", email = "marc@marclehmann.name" }]
license = "AGPL-3.0-or-later"
license-files = ["LICENSE"]
readme = "README.md"
requires-python = ">=3.14"
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Environment :: Console",
    "Environment :: Web Environment",
]
dependencies = [
    "fastmcp>=3.2.4, <4",
    "fastapi>=0.136.1",
    "matrix-nio[e2e]>=0.25.2",
    "openrouter==0.9.2",
    "pydantic>=2.13.3, <3",
    "ruamel.yaml>=0.19.1",
    "uvicorn[standard]>=0.46.0",
    "whenever>=0.10.0",
]

[project.optional-dependencies]
dev = [
    "yapf>=0.43.0",
]
test = [
    "pyhamcrest>=2.1.0",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
]

[project.urls]
source = "https://github.com/dddsnn/clawp"

[project.scripts]
clawp = "clawp:run"

[tool.setuptools.package-data]
clawp = ["file/message_templates/**/*", "file/personalities/**/*"]

[tool.uv]
compile-bytecode = true

[tool.pytest]
minversion = "9.0"
addopts = ["-v", "-l"]
asyncio_mode = "auto"
testpaths = ["tests"]
log_level = "debug"
